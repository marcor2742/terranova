import { MetricToImperialPipe } from './metric-to-imperial.pipe';

describe('MetricToImperialPipe', () => {
  it('create an instance', () => {
    const pipe = new MetricToImperialPipe();
    expect(pipe).toBeTruthy();
  });
});
